v7.0.2
- fix overlap add for high frequencies (just in case)
- fixed potential latency problems when host looped
- fixed bypass latency compensation on ProTools (and possibly some other hosts)

v7.0.1 (released)
- enabled bypass feature in Protools

v7.0.0 (released)
- improved compatibility
- optimized performances
- fixed spectrum values that were not exactly correct
- optimized ratio and noise only
- fixed transient boost
